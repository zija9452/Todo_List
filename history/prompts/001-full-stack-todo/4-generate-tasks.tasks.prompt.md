---
id: 4
title: Generate tasks
stage: tasks
date: 2026-02-09
surface: agent
model: Claude Haiku 4.5
feature: 001-full-stack-todo
branch: 001-full-stack-todo
user: user
command: /sp.tasks
labels: ["tasks", "full-stack", "todo-app", "implementation"]
links:
  spec: specs/001-full-stack-todo/spec.md
  plan: plans/plan_002_task-crud-auth.md
  tasks: specs/001-full-stack-todo/tasks.md
  ticket: null
  adr: null
  pr: null
files:
  - "specs/001-full-stack-todo/tasks.md"
tests: []
---

## Prompt

/sp.tasks
"""
Project: Evolution of Todo — Phase II (Full-Stack Web) — Claude Code Task Prompt

Context: Phase II only (`current_phase: 2`, `phase_status: in_progress`, `scope_limit: phase_2_only`). Claude Code MUST follow spec-first workflow: create/update specs → create plan → execute tasks. All production code must be generated by Claude Code agents. Human edits allowed only for README, .env.example, and CLAUDE.md unless a spec explicitly permits otherwise.

Task list (atomic, ordered, agent-executable). Each task entry: ID, Action, Target files, Expected outputs, Tests, Commit message pattern, Estimated effort (S/M/L), Dependencies.

T001 — Finalize Task-CRUD Spec

* Action: Update `specs/features/task-crud.md` with exact API contract, data schema, query params, error codes, and AC.
* Files: `specs/features/task-crud.md`
* Output: spec v1.1 (Gherkin AC).
* Tests: N/A (spec change).
* Commit: `spec(002-task-crud): finalize API contract and AC`
* Effort: S
* Depends: none

T002 — Add Authentication Spec

* Action: Create/update `specs/features/authentication.md` with JWT details, claim names, ownership rules, and acceptance criteria.
* Files: `specs/features/authentication.md`
* Output: spec v1.0
* Commit: `spec(002-auth): add authentication spec`
* Effort: S
* Depends: T001

T003 — Create Plan Artifact

* Action: Write `plans/plan_002_task-crud-auth.md` with milestones, atomic subtasks (this task list), tests, env, and acceptance checklist.
* Files: `plans/plan_002_task-crud-auth.md`
* Output: plan file
* Commit: `chore(plan): add plan_002_task-crud-auth`
* Effort: S
* Depends: T001,T002

T010 — Backend: Add SQLModel Task Model + Migrations Scaffold

* Action: Generate `backend/src/models.py` (SQLModel Task model) and migrations scaffold (alembic or recommended tool per spec).
* Files: `backend/src/models.py`, `backend/alembic/*` or migration notes
* Output: model file, migration scaffold, model unit test `backend/tests/test_models.py`
* Tests: schema validation tests (fields/types/constraints)
* Commit: `feat(002-model): add Task SQLModel + migrations scaffold`
* Effort: M
* Depends: T003

T011 — Backend: DB Connection & Neon Setup

* Action: Create `backend/src/db.py` to connect using `DATABASE_URL` env var and include `.env.example` entries.
* Files: `backend/src/db.py`, `.env.example` (update)
* Output: DB connection helper & example env
* Tests: DB connection smoke test (mock or use test DB config) `backend/tests/test_db.py`
* Commit: `chore(db): add Neon connection helper`
* Effort: S
* Depends: T010

T012 — Backend: JWT Auth Middleware

* Action: Implement `backend/src/auth.py` middleware to extract and verify JWT using `BETTER_AUTH_SECRET` (spec must justify chosen JWT lib). Attach `current_user` to requests.
* Files: `backend/src/auth.py`, tests `backend/tests/test_auth.py`
* Output: middleware + token verification tests (valid/expired/invalid)
* Tests: token verification, claim extraction, expiry handling
* Commit: `feat(002-auth): add JWT verification middleware`
* Effort: M
* Depends: T011, T002

T013 — Backend: CRUD Routes + Ownership Enforcement

* Action: Implement routes under `backend/src/routes/tasks.py` for all endpoints; enforce `user_id === token.sub`. Use Pydantic response/request models.
* Files: `backend/src/routes/tasks.py`, `backend/src/schemas.py`, tests `backend/tests/test_routes_*.py`
* Output: full CRUD endpoints + PATCH complete + unit tests for 401/403/404/422 cases
* Tests: unit tests for each endpoint, mocking auth and DB
* Commit: `feat(002-crud): implement task endpoints with auth`
* Effort: L
* Depends: T012, T010

T014 — Backend: Integration Tests (Auth + DB)

* Action: Create integration tests using test secret to sign tokens, run against test DB (or test container).
* Files: `backend/tests/test_integration_auth_flow.py`
* Output: e2e integration tests: create->list->update->delete with ownership checks
* Tests: integration suite; required for phase acceptance
* Commit: `test(002-integration): add integration tests`
* Effort: L
* Depends: T013, T011

T020 — Frontend: Better Auth Integration (JWT issuance)

* Action: Configure `frontend` Better Auth setup to request JWT and store session; ensure `NEXT_PUBLIC_AUTH_ORIGIN` in `.env.example`.
* Files: `frontend/CLAUDE.md` (update), `frontend/lib/auth.ts` or appropriate client hooks
* Output: client that can obtain JWT and expose token to API client
* Tests: smoke test for login flow (can mock Better Auth) `frontend/tests/test_auth_flow.*`
* Commit: `feat(002-frontend-auth): integrate Better Auth`
* Effort: M
* Depends: T002

T021 — Frontend: API Client (attach Authorization header)

* Action: Create `frontend/lib/api.ts` to call backend endpoints including token header from Better Auth session.
* Files: `frontend/lib/api.ts`, tests `frontend/tests/test_api_client.*`
* Output: api client wrappers for CRUD endpoints
* Tests: unit tests asserting header attachment and request shapes (mock fetch)
* Commit: `feat(002-frontend-api): add API client`
* Effort: S
* Depends: T020

T022 — Frontend: Tasks UI (Dashboard, Create, Edit, Detail)

* Action: Implement pages/components: `frontend/app/(dashboard)/page.tsx`, `frontend/app/tasks/new/page.tsx`, `frontend/app/tasks/[id]/page.tsx`. Use server/client components per CLAUDE.md.
* Files: frontend components/pages, tests `frontend/tests/*`
* Output: responsive UI to perform CRUD and toggle completion, wired to API client
* Tests: smoke E2E (Playwright or RTL) for main flows
* Commit: `feat(002-frontend-ui): implement tasks UI`
* Effort: L
* Depends: T021

T030 — Frontend: Handle Auth Errors (401 redirect)

* Action: Add global error handling to redirect to login on 401 and show friendly messages.
* Files: `frontend/app/layout.tsx` or error boundary, tests `frontend/tests/test_401_handling.*`
* Output: 401 handling and UX
* Commit: `chore(002-frontend-auth): add 401 handling`
* Effort: S
* Depends: T022

T040 — CI & Docs

* Action: Add backend CI workflow (`backend/.github/workflows/ci.yml`) running `pytest`; add frontend checks in monorepo CI; update root `README.md` and `CLAUDE.md` with run/test commands and `current_phase` metadata.
* Files: `.github/workflows/backend-ci.yml`, `.github/workflows/frontend-ci.yml` (optional), `README.md`, `CLAUDE.md` updates
* Output: CI config + docs + `.env.example` updated
* Tests: CI should run tests successfully (configured)
* Commit: `chore(ci): add CI for backend/frontend`
* Effort: M
* Depends: T014, T022

T050 — Persistence Verification (Neon)

* Action: Document and run a local persistence test (or CI step) that writes tasks, restarts backend, and verifies data remains in Neon. Provide instructions in README.
* Files: `README.md` persistence test section, optional script `scripts/persist_test.sh`
* Output: documented persistence verification + evidence (test logs)
* Commit: `test(002-persist): verify Neon persistence`
* Effort: M
* Depends: T011, T013

T060 — Finalize CLAUDE.md & Phase Gate

* Action: Generate `CLAUDE.md` trace: for each spec/plan/task include prompt, plan, generated files, and test outputs. Set `phase_status` to `in_progress` until acceptance. Provide checklist for reviewer.
* Files: `CLAUDE.md` (root), `frontend/CLAUDE.md`, `backend/CLAUDE.md`
* Output: full trace and reviewer checklist
* Commit: `docs(claude): add implementation trace`
* Effort: S
* Depends: All above

T070 — Acceptance Verification & Phase Gate Close

* Action: Run full test suite; verify acceptance checklist (endpoints, auth, persistence, tests pass). If all good, mark acceptance and update `plans/` and metadata `phase_status: accepted` only upon reviewer confirmation.
* Files: `plans/plan_002_task-crud-auth.md` (update), root metadata change (upon acceptance)
* Output: test reports, updated plan metadata
* Commit: `chore(002-accept): mark phase 2 acceptance` (only after manual review)
* Effort: S
* Depends: T040, T050, T060

Standards & conventions (repeat for agent)

* Commit message pattern: include spec/plan id: e.g. `feat(002-crud): implement task endpoints`. Atomic commits only.
* Tests required for each feature and must pass locally before committing final feature commit.
* Do not commit secrets; update `.env.example` only.
* Any added dependency must be justified in its spec and added to `backend/pyproject.toml` or `frontend/package.json` with a plan entry.
* Use `BETTER_AUTH_SECRET`, `DATABASE_URL`, `NEXT_PUBLIC_AUTH_ORIGIN` as required env names.

Priority & run order

* Priority: T001 → T003 → T010 → T011 → T012 → T013 → T014 → T020 → T021 → T022 → T030 → T040 → T050 → T060 → T070
* First actionable task for Claude Code agent: **T001 — Finalize Task-CRUD Spec**, then T002, then T003.

Deliverable format

* For each completed task, produce: commit(s), generated files (paths), tests, and a one-paragraph CLAUDE.md summary referencing the spec and plan id.

Return behavior

* Do not proceed to Phase III tasks. If any spec/plan ambiguity is found while executing a task, create a spec update (new version) and block further changes until the spec is recorded and committed.

END OF /sp.tasks
"""

## Response snapshot

Successfully generated the comprehensive task list for the Full-Stack Multi-User Todo Application implementation. The tasks.md file contains:

1. **Structured phases** following the user story priorities (P1, P2, P3) from the specification
2. **50 atomic tasks** (T001-T050) organized in dependency order
3. **Parallel execution opportunities** clearly marked with [P] tags
4. **User story mapping** with [US1], [US2], [US3] tags for traceability
5. **Complete coverage** of frontend and backend requirements including authentication, CRUD operations, UI, testing, CI/CD, and documentation
6. **Detailed task descriptions** with specific file paths for implementation

The task list aligns with all requirements from the original prompt, covering the backend FastAPI + SQLModel implementation, Neon Postgres persistence, Better Auth JWT integration, secured REST API endpoints, comprehensive testing strategy, and proper documentation.

## Outcome

- ✅ Impact: Created detailed task breakdown for Phase II implementation with 50 atomic tasks
- 🧪 Tests: Included comprehensive testing tasks as required by spec
- 📁 Files: specs/001-full-stack-todo/tasks.md created with proper structure
- 🔁 Next prompts: Ready for implementation using the generated tasks
- 🧠 Reflection: Task list enables parallel execution and independent story delivery

## Evaluation notes (flywheel)

- Failure modes observed: None
- Graders run and results (PASS/FAIL): PASS - all required tasks generated with proper structure
- Prompt variant (if applicable): N/A
- Next experiment (smallest change to try): Begin implementation with first tasks (T001-T005)